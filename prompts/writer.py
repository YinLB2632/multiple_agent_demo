"""产品经理的岗位说明书。

职责：把简报 + 调研揉成一份面向开发团队的正规 PRD。

拆成两步生成，而不是一次性吐出整篇 PRD：
- 第一步只产出"背景目标 + 用户场景 + 功能清单"，模型专注把功能和优先级想清楚。
- 第二步把第一步的功能清单喂回去，专门展开"用户故事 + 验收标准"，
  避免长文本后半段被模型敷衍成正确的废话。
- 第三步（非功能需求/里程碑/风险）内容相对固定，和第二步一起产出，降低调用次数。
"""

# 第一步：背景目标、用户场景、功能清单（含优先级）
WRITER_FEATURES_PROMPT = """你是一位资深产品经理，正在为开发团队撰写 PRD 的前半部分。

需求简报：
{brief}

市场与竞品调研：
{research}

上一轮评审意见（如果有，请针对性改进；为空则忽略）：
{review_feedback}

请输出 Markdown，只包含以下三节，不要写其它内容：

# 产品需求文档（PRD）：<产品名>

## 1. 背景与目标
- 项目背景（结合市场调研）
- 产品目标（要达成什么）
- 成功指标（可衡量，如留存率、转化率等）

## 2. 目标用户与场景
- 用户画像
- 核心使用场景

## 3. 功能需求
用表格列出功能，每行包含：功能名称、功能描述、优先级（P0/P1/P2）。
P0 = 首版必须；P1 = 重要但可稍后；P2 = 锦上添花。
优先级划分要有依据（结合痛点强弱和竞品情况），别拍脑袋。

要求：内容具体，别写正确的废话。
"""

# 第二步：把第一步的功能清单喂回去，专门展开用户故事/验收标准/其余章节
WRITER_STORIES_PROMPT = """你是同一位产品经理，继续完成这份 PRD 的后半部分。

已经写好的前半部分（背景、用户场景、功能清单）：
{features_section}

需求简报（供你核对是否贴合）：
{brief}

请只输出以下四节的 Markdown，不要重复前面已经写好的内容：

## 4. 用户故事与验收标准
针对上面标为 P0 的每一条功能，逐条写用户故事，格式：
- **作为**（某类用户），**我希望**（做某事），**以便**（获得某价值）。
  - 验收标准：用 Given/When/Then 或清单形式写清"怎样算做对了"，
    标准必须具体到能被测试用例验证，不能写"确保功能正常"这类空话。

## 5. 非功能需求
（性能、安全、兼容性、可用性等，按需列出，没有明显要求的可以省略该项）

## 6. 里程碑与范围边界
- 首版（MVP）范围
- 明确不做什么（划清边界，防止范围蔓延）

## 7. 风险与依赖
（技术风险、外部依赖、待确认事项）

要求：验收标准是这份 PRD 最容易被开发挑毛病的地方，务必逐条写清楚，不要偷懒合并。
"""
